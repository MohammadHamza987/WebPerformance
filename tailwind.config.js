/**
 * Tailwind CSS v4 uses on-demand compilation and no longer requires a `content` array.
 * Keep this file only if you need to customize the default theme or add plugins.
 */
export default {
  // Add theme extensions or plugins here if/when needed.
};
